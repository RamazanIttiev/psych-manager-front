const initialState = {
  name: '',
  age: '',
  gender: '',
  skills: [],
  about: '',
};

const newUserReducer = (state = initialState, action) => {
  switch (action.type) {
    case example:
  }
};
